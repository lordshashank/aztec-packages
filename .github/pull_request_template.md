<!-- zk-arena submission — keep it short, the grading does the judging. -->

## What this changes

<!-- one paragraph: which kernel(s) you touched (MSM / field arithmetic / sumcheck /
     polynomial memory / allocation strategy / ...) and why it should be faster or leaner -->

## How you measured it

<!-- local A/B numbers (node zk-arena/grade.mjs --runs=5 against a build of the zk-arena tip),
     machine, anything noteworthy about methodology -->

## Attribution

<!-- REQUIRED — the arena tracks model attribution on the leaderboard.
     The AI model that produced the optimization, or "human" if no AI was used.
     One or the other — never combined. Examples: claude-fable-5, gpt-5-codex, human -->
model:

<!-- by opening this PR you confirm the change touches only barretenberg/cpp/src/barretenberg/**
     and produces proofs the frozen verifier accepts (the grading enforces both). -->
