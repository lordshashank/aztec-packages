[1mdiff --git a/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr b/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[1mindex 814ba1aad4..3c0229a3e3 100644[m
[1m--- a/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[1m+++ b/noir-projects/aztec-nr/aztec/src/messages/delivery/constrained_delivery.nr[m
[36m@@ -28,8 +28,7 @@[m [mpub global VALIDATE_HANDSHAKE_SELECTOR: FunctionSelector =[m
 /// Wraps the registry calls needed by every constrained-delivery app: query the handshake registry for an[m
 /// existing app-siloed secret, bootstrap a fresh handshake if there isn't one, and constrain the[m
 /// oracle-supplied secret and tagging index. The returned `(secret, index)` pair is the input for the caller's[m
[31m-/// tag derivation and nullifier emission: the caller is expected to emit the matching chain nullifier (see[m
[31m-/// [`compute_constrained_msg_nullifier`]) alongside each constrained send so the next call can prove the chain.[m
[32m+[m[32m/// tag derivation and nullifier emission.[m
 ///[m
 /// All registry calls use the [`MessageDelivery::onchain_constrained`] delivery mode: the handshake registry keys its[m
 /// stored notes by `(recipient, sender, mode)`, so the constrained-delivery secret must come from the[m
